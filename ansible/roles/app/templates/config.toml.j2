# Generated by Ansible — do not edit manually.
# DB host is taken from the db VM's IP via the Ansible inventory.

[server]
host = "127.0.0.1"
port = {{ app_port }}

[database]
host = "{{ db_host_ip }}"
port = {{ db_port }}
name = "{{ db_name }}"
user = "{{ db_user }}"
password = "{{ db_password }}"
